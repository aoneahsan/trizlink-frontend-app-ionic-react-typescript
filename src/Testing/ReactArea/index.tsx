// Core Import

// Packages Imports

// Custom Imports

const ZaionsReactArea: React.FC = () => {
  // const [events, setEvents] = useState<string[]>([]);
  // const { isFocusVisible } = useFocusVisible({ isTextInput: true });

  // const { focusProps } = useFocus({
  // 	onFocus: (e) =>
  // 		setEvents((events) => {
  // 			return [...events, 'focus'];
  // 		}),
  // 	onBlur: (e) =>
  // 		setEvents((events) => {
  // 			return [...events, 'blur'];
  // 		}),
  // 	onFocusChange: (isFocused) =>
  // 		setEvents((events) => {
  // 			return [...events, `focus change: ${isFocused}`];
  // 		}),
  // });
  return (
    <>
      {/* <ZIonItem>
				<ZIonLabel>Example of useFocus in React aria</ZIonLabel>
				<input {...focusProps} id='example' />
			</ZIonItem> */}
      {/* <ZIonList>
				{events.map((e, i) => (
					<ZIonItem key={i}>{e}</ZIonItem>
				))}
			</ZIonList> */}
    </>
  );
};

export default ZaionsReactArea;
